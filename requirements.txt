# Synapse 2.0 - Core runtime dependencies
google-generativeai>=0.3.0
numpy>=1.26.0

# Optional: fast vector retrieval backend (falls back gracefully if unavailable)
faiss-cpu>=1.8.0; platform_system != "Darwin" or platform_machine != "arm64"
